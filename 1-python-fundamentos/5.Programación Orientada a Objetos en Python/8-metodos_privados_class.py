# __init__(self): Constructor de la clase. Es llamado cuando se crea una nueva instancia de la clase. Inicializa los atributos del objeto.
# __str__(self): Devuelve una representación en cadena del objeto, utilizada por print() y str(). Este método es útil para proporcionar una representación legible del objeto.
# __repr__(self): Devuelve una representación “oficial” del objeto, utilizada por repr(). Este método está diseñado para devolver una cadena que represente al objeto de manera que se pueda recrear.
# __len__(self): Devuelve la longitud del objeto. Se puede llamar a la función len() en el objeto.
# __del__(self): Se llama cuando el objeto va a ser destruido. Se utiliza para limpiar las referencias a los objetos.
# __add__(self, other): Devuelve la suma de self y other. Se puede llamar al operador + en el objeto.
# __sub__(self, other): Devuelve la resta de self y other. Se puede llamar al operador - en el objeto.
# __mul__(self, other): Devuelve el producto de self y other. Se puede llamar al operador * en el objeto.
# __truediv__(self, other): Devuelve la división de self y other. Se puede llamar al operador / en el objeto.
# __floordiv__(self, other): Devuelve la división entera de self y other. Se puede llamar al operador // en el objeto.
# __mod__(self, other): Devuelve el módulo de self y other. Se puede llamar al operador % en el objeto.
# __pow__(self, other): Devuelve la potencia de self y other. Se puede llamar al operador ** en el objeto.
# __eq__(self, other): Comprueba si self y other son iguales. Se puede llamar al operador == en el objeto.
# __ne__(self, other): Comprueba si self y other no son iguales. Se puede llamar al operador != en el objeto.
# __lt__(self, other): Comprueba si self es menor que other. Se puede llamar al operador < en el objeto.
# __gt__(self, other): Comprueba si self es mayor que other. Se puede llamar al operador > en el objeto.
# __le__(self, other): Comprueba si self es menor o igual que other. Se puede llamar al operador <= en el objeto.
# __ge__(self, other): Comprueba si self es mayor o igual que other. Se puede llamar al operador >= en el objeto.
# __contains__(self, item): Comprueba si item está contenido en self. Se puede llamar al operador in en el objeto.
# __getitem__(self, key): Devuelve el valor del elemento con clave key. Se puede llamar al operador [] en el objeto.
# __setitem__(self, key, value): Establece el valor del elemento con clave key. Se puede llamar al operador [] en el objeto.
# __delitem__(self, key): Elimina el elemento con clave key. Se puede llamar al operador del en el objeto.
# __iter__(self): Devuelve un iterador para el objeto. Se puede llamar a la función iter() en el objeto.


